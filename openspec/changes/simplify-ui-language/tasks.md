## 1. 仕様と表示

- [x] 1.1 #46 の承認・据え置き範囲と平均色 UI を OpenSpec delta に記録し、strict validation を通す。
- [x] 1.2 `web/index.html` の seed・色数値・平均色欄とアクセシビリティ表示を整理し、二つのモード名と勝利ポップアップは維持する。
- [x] 1.3 `web/styles.css` に目盛りと駒を実装し、0/255と狭い画面で欠けない配置にする。

## 2. なでしこの状態表示

- [x] 2.1 `web/main.nako3` の承認済み文言を初見向けに置換し、内部 `dark/light`、通信詳細、専門語を通常画面に出さない。
- [x] 2.2 終局盤面の平均値を目盛り位置と駒色へ反映する。勝敗計算と指定勝利文言は変更しない。
- [x] 2.3 seed の既定新規対局を残し、`?debug=1` で旧来の seed 手動入力を使えるようにする。

## 3. 確認

- [x] 3.1 `docs/manual-debug.md` に通常画面と debug 画面の確認項目を更新する。
- [x] 3.2 WSL `make check` と OpenSpec strict validation を通す。
- [ ] 3.3 文言・数直線・対戦切替の人間による画面確認を PR に引き継ぐ。
