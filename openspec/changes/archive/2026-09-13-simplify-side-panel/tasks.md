## 1. 右側の表示構造

- [x] 1.1 `web/index.html`から手数・合法手・自分の項目とrequestセクションを削除し、DOMにそれらの見出し・IDがなく、手番・seed・新しい対局・パス・状態欄・終局ポップアップが残ることを確認する
- [x] 1.2 `web/styles.css`で次色プレビューを直径64px・黒輪郭の円にし、0と255の塗りでも盤上の駒と同じ輪郭で識別でき、通常幅と640px未満でレイアウトが崩れないことをブラウザで確認する
- [x] 1.3 requestプレビュー専用CSSを削除し、`.command-preview`が残らないことを検索で確認する

## 2. なでしこの表示更新

- [x] 2.1 `web/main.nako3`から削除済みの手数・合法手・自分DOMへの更新を除き、`#meta-turn-player`と次色の円・数値は確定stateで更新されることをブラウザで確認する
- [x] 2.2 requestプレビュー専用の状態・JSON整形・`#command-preview`更新を除き、実APIで新規対局と着手、パス可能fixtureでパス要求と画面復帰を確認し、ルール側の`--test`でパス成功を確認する
- [x] 2.3 `turnNumber`と`legalMoves`の内部利用を保ち、削除したDOM IDへの参照がゼロで、合法手とパス可否の判定が維持されることを検索とブラウザで確認する

## 3. 手動回帰と受け入れ条件

- [x] 3.1 初期state・着手後state・終局stateで次色、手番、勝敗ポップアップを手動確認し、色値0と255を含むstateでも円形と数値が正しいことを確認する
- [x] 3.2 パス可能stateと範囲外seed・通信失敗を手動確認し、request欄なしでも操作と状態・エラー表示が維持されることを確認する
- [x] 3.3 WSLで`make check`を実行し、Issue #35の受け入れ条件をPR本文で一つずつ確認する
