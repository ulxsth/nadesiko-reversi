## 1. 棋譜の直列化

- [x] 1.1 契約の6語に対応する行の生成関数を作り、生成した行をそのまま走査で読み直せることを確認する
- [x] 1.2 再生結果から棋譜全体を書き出し、注釈に駒色・変換数・総手数・色合計・駒数を書く
- [x] 1.3 `gameId`を`[A-Za-z0-9_-]{1,64}`に制限し、棋譜のファイル名を導けることを確認する

## 2. 再生ハーネス

- [x] 2.1 `rules/replay/harness.nako3`に契約の6語を定義し、対局データをJSONで返せることを確認する
- [x] 2.2 契約の例をそのまま読み、gameId・seed・勝者・指し手・注釈・行番号が取れることを確認する
- [x] 2.3 ハーネスと棋譜を連結して実行し、gonakoの診断の行番号を棋譜側へ戻せることを確認する

## 3. 再生と検証

- [x] 3.1 棋譜を#3のinterface経由で再適用し、各手の盤面と指定手数の盤面を返せることを確認する
- [x] 3.2 記録された勝者と再生結果が一致することを確認し、不一致を`record_mismatch`にする
- [x] 3.3 文法違反・開始行の欠落と重複・終了行の欠落・ルール拒否・注釈の不一致を、行番号とソースつきで拒否する
- [x] 3.4 実対局から作ったサンプル棋譜が、再生と書き出しの往復で元と一致することを確認する

## 4. 保存と取り出し

- [x] 4.1 棋譜ソースを正本として保持するStore interfaceとプロセス内実装を作る
- [x] 4.2 一覧・ID取得・ランダム1件取得のservice testを書き、保存順と複製の独立性を確認する
- [x] 4.3 改ざんされた棋譜が保管庫へ入らないことを確認する

## 5. 検証

- [x] 5.1 `go vet`と`go test -race`が成功することを確認する
- [x] 5.2 サンプル棋譜が人間に読める日本語コードになっていることを確認する
- [x] 5.3 `make check`を実行し、Issue #6の受け入れ条件を一つずつ確認する
