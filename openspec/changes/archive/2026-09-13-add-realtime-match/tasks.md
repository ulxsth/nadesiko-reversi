## 1. マッチング

- [x] 1.1 先着二人を1つのroomへ割り当て、1人目へ待機、二人揃った時点で両者へ成立と初期stateを配信する
- [x] 1.2 満席のroomには入れず、2人目の参加処理中に3人目が来た場合も新しいroomを開くことを確認する
- [x] 1.3 席をdark、lightの順に埋め、席と参加者の対応を返し、同じplayerIDの同時Joinで席を重複割り当てしないことを確認する

## 2. 確定と配信

- [x] 2.1 ルール評価を#3の`runtime.Runner`だけに通し、確定stateをroomが保持する
- [x] 2.2 受理のたびに差分ではなくstate全体を全席へ配信し、盤面・次手番・次の駒色・phaseが届くことを確認する
- [x] 2.3 配信channelが詰まったら捨て、対局を止めないことを確認する

## 3. 順序と拒否

- [x] 3.1 同一`expectedTurn`の2件目を`stale_turn`で拒否し、盤面と手番が動かないことを確認する
- [x] 3.2 `commandId`を記憶せず、異なる`commandId`でも古い`expectedTurn`なら拒否されることを確認する
- [x] 3.3 別room、非参加者、席違い、対局開始前、終局後のcommandを拒否する
- [x] 3.4 拒否をroomへ配信せず、送信者へ変更前stateを含むresponseだけを返すことを確認する

## 4. 切断と寿命

- [x] 4.1 切断で席を残したままchannelを閉じ、roomを再接続待ちへ移し、相手へ通知する
- [x] 4.2 同じplayerIDの再接続で席を復帰させ、現在state全体を1通配信する
- [x] 4.3 二人とも切断したroomを破棄し、roomと参加者の対応が残らないことを確認する

## 5. 検証

- [x] 5.1 transportを使わないpackage testで全経路を確認する
- [x] 5.2 実ルールengineで対局を終局まで進め、契約の固定vectorと一致することを確認する
- [x] 5.3 `go test -race`で共有状態の競合がないことを確認する
- [x] 5.4 `make check`を実行し、Issue #5の受け入れ条件を一つずつ確認する
