## 1. 状態の描画

- [x] 1.1 契約のstate JSONから8×8盤面と0〜255の駒色を描き、初期配置4駒と中盤の階調が出ることを確認する
- [x] 1.2 合法手・キーボードカーソル・選択中のマスを、色ではなく形（四角・枠・四隅の印）で描き分ける
- [x] 1.3 駒の輪郭を墨色に統一し、白い駒が白い盤に沈まないことを確認する
- [x] 1.4 canvasへ渡る色が白・黒・グレースケールだけであることを確認する

## 2. 表示状態

- [x] 2.1 手番、相手待ち、終了、エラー、送信待ちを日本語で表示し、色調を切り替える
- [x] 2.2 次の駒色、手番、手数、合法手数、自分のplayerをパネルへ出す
- [x] 2.3 パスは合法手0件の自分の手番でだけ押せるようにする

## 3. 入力からcommandへ

- [x] 3.1 クリック・タップの座標をCanvas解像度へ換算し、対応するマスへ変換することを確認する
- [x] 3.2 矢印キーでカーソルを動かし、Enter・スペースで着手、Pでパスできるようにする
- [x] 3.3 着手とパスを契約のcommandへ組み立て、送信予定の内容を画面で確認できるようにする
- [x] 3.4 手番違い、非合法手、終了後の操作、合法手があるときのパスを送信前に日本語で拒否する

## 4. 通信境界とfixture

- [x] 4.1 サーバーへの出口を`ルール要求送信`1関数に閉じ、fixture表示では送信しないことを確認する
- [x] 4.2 着手しても確定応答までUIの盤面が変わらないことを確認する
- [x] 4.3 実ルールengineの出力から開始直後・序盤・中盤・パスのみ・終了のfixtureを作る
- [x] 4.4 fixtureの切り替えと自分playerの切り替えで主要状態を手動確認できるようにする

## 5. 画面と検証

- [x] 5.1 640px未満で盤とパネルが縦に積まれ、盤が画面幅に収まるようにする
- [x] 5.5 書体をDotGothic16にし、角丸を使わず、背景以外を白黒にする
- [x] 5.6 キャッチコピー・操作説明・開発用リンク・座標ラベルを画面から外す
- [x] 5.2 Canvasをフォーカス可能にし、フォーカスリングとキーボード操作の説明を出す
- [x] 5.3 jsdom上でコンパイル・描画・操作・拒否表示を自動確認する
- [x] 5.4 `make check`が成功することを確認し、Issue #4の受け入れ条件を一つずつ確認する
