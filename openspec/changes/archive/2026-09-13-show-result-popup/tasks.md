## 1. ポップアップ要素と表示スタイル

- [x] 1.1 `web/index.html`へ初期状態が`hidden`の終局live regionを1個追加し、DOM上に重複しないこととARIA属性をブラウザ開発者ツールで確認する
- [x] 1.2 `web/styles.css`へ画面中央の白地・黒枠・角丸なしの表示を追加し、幅640px未満と通常幅の両方で中央に収まることを確認する

## 2. state連動の表示制御

- [x] 2.1 `web/main.nako3`へ`dark`と`light`を黒と白へ変換する終局文言処理を追加し、finished fixtureの双方のwinner値で指定文言と完全一致することを確認する
- [x] 2.2 既定表示更新から同じ静的要素の表示・非表示を切り替え、同じfinished stateの再描画で要素数が1のまま変わらないことを確認する
- [x] 2.3 不正なwinnerではポップアップを隠して状態欄をエラーへ変え、誤った勝者文言が表示されないことを確認する
- [x] 2.4 新しい対局の操作時にresponse到着前からポップアップを隠し、その後のplaying stateでも非表示が維持されることを確認する

## 3. 回帰確認

- [x] 3.1 `web/fixtures/finished.json`と実際の終局responseが同じ表示経路を通り、既存の盤面・状態欄・新しい対局操作が維持されることを手動確認する
- [x] 3.2 `alert()`が追加されていないこと、表示要素がlive regionとして勝敗文全体を持つことを確認する
- [x] 3.3 WSLで`make check`を実行し、Issue #34の受け入れ条件を一つずつPR本文で確認する
